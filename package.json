{
  "name": "diveBuddy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n shared,backend,frontend -c yellow,blue,green \"pnpm --filter @shared dev\" \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "vercel-build": "pnpm --filter @shared... build && pnpm build",
    "build": "nuxt build"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
